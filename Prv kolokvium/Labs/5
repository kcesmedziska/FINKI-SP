#include <stdio.h>
#include <stdlib.h>

int main()
{
    int x;
    scanf("%d", &x);
    
    int a=(x/100000), b=((x/10000)%10), c=((x/1000)%10), d=((x/100)%10), e=((x%100)/10), f=(x%10);
    
    if(a==f && b==e && c==d)
        printf("1");
    else 
        printf("0");
    
    return 0;
}
