#include <stdio.h>
#include <stdlib.h>


int main()
{
    int n,i,a[100];
    scanf("%d", &n);
    for(i=0;i<n;i++)
        scanf("%d", &a[i]);
    for(i=0;i<n;i++)
        if(a[i]%2==0)
            printf("%d ", a[i]+1);
        else 
            printf("%d ", a[i]-1);
    
    return 0;
}
